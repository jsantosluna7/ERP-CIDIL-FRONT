import { AppCualRolDirective } from './app-cual-rol.directive';

describe('AppCualRolDirective', () => {
  it('should create an instance', () => {
    const directive = new AppCualRolDirective();
    expect(directive).toBeTruthy();
  });
});
